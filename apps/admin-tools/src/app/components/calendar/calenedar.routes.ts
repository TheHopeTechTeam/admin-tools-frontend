import { Routes } from '@angular/router';
import { CalenedarIndexComponent } from './index/index.component';

export const CalendarRoutes: Routes = [
    { path: 'index', component: CalenedarIndexComponent }
];